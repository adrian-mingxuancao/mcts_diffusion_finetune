# MCTS Hallucination Pipeline Requirements
# Install with: pip install -r requirements.txt

# Core dependencies
numpy>=1.24.0
torch>=2.0.0
transformers>=4.40.0

# Structure prediction
biopython>=1.80

# ESMFold (optional - for ESMFold backend)
# Requires: pip install torch transformers
# Model: facebook/esmfold_v1

# Boltz (optional - for Boltz backend)
# Install from: https://github.com/jwohlwend/boltz
# boltz>=0.3.0

# Chai-1 (optional - for Chai backend)
# Install from: https://github.com/chaidiscovery/chai-lab
# chai-lab>=0.5.0

# ProteinMPNN (required for inverse folding)
# Clone from: https://github.com/dauparas/ProteinMPNN
# Set PROTEINMPNN_PATH environment variable

# DSSP (required for secondary structure analysis)
# Install with micromamba:
#   micromamba create -p /net/scratch/$USER/dssp_env -c conda-forge dssp
# Binary path: /net/scratch/$USER/dssp_env/bin/mkdssp

# Visualization and analysis
matplotlib>=3.5.0
pandas>=1.5.0
scipy>=1.10.0

# Testing
pytest>=7.0.0

# Optional: ABCFold wrapper for AF3/Boltz/Chai
# cd ABCFold && pip install -e .
